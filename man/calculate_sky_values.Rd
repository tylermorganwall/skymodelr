% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_sky.R
\name{calculate_sky_values}
\alias{calculate_sky_values}
\title{Sample a direction from the Prague model.}
\usage{
calculate_sky_values(
  phi,
  theta,
  altitude = 0,
  elevation = 10,
  visibility = 50,
  albedo = 0.5,
  azimuth = 90,
  numbercores = 1,
  wide_spectrum = FALSE,
  solar_disk = TRUE
)
}
\arguments{
\item{phi}{Horizontal angle of the sample, degrees. Vectorized. Range \link{0, 360}.}

\item{theta}{Vertical angle of the sample, degrees. Vectorized. Range \link{-90, 90}.}

\item{altitude}{Default \code{0}, vectorized. Altitude of the viewer in meters. Range \link{0, 15000}.}

\item{elevation}{Default \code{10}, vectorized. Solar elevation angle above/below the horizon (degrees). Range \link{-4.2, 90}.}

\item{visibility}{Default \code{50}, vectorized. Range \link{20, 131.8}. Meteorological range in kilometers for Prague model.}

\item{albedo}{Default \code{0.5}, vectorized. Range \link{0, 1}. Ground albedo.}

\item{azimuth}{Default \code{90}, single value. Solar azimuth (degrees). Defaults South.}

\item{numbercores}{Default \code{1}. Number of threads to use in computation.}

\item{wide_spectrum}{Default \code{FALSE}. Whether to use the wide‑spectrum (55‑channel, polarised) coefficients.}

\item{solar_disk}{Default \code{TRUE}. Whether to sample the solar disk as well.}
}
\value{
3 column RGB matrix.
}
\description{
Evaluate the Prague spectral sky model at arbitrary spherical
directions without writing an image, returning radiance-only samples.
}
\examples{
#Generate a basic atmosphere with the Hosek model
if(run_documentation()) {
value_grid = expand.grid(
  phi = seq(0,360,by=30),
  theta = seq(0,90,by=10),
  altitude = c(0,10000)
 )
vals = calculate_sky_values(
  phi = value_grid$phi,
  theta = value_grid$theta,
  altitude = value_grid$altitude,
  elevation = 45,
  visibility = 120,
  albedo = 0
 )
 cbind(value_grid, vals)
}
}
