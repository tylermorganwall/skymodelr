% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_prague_sky_metadata.R
\name{get_prague_sky_metadata}
\alias{get_prague_sky_metadata}
\title{Prepare Location and Time Metadata for Native Prague Sky Queries}
\usage{
get_prague_sky_metadata(
  datetime,
  lat,
  lon,
  altitude = 0,
  visibility = 50,
  albedo = 0.5,
  prague_rgb_correction = TRUE,
  prague_rgb_correction_strength = 1,
  prague_rgb_correction_gain = "auto"
)
}
\arguments{
\item{datetime}{A single \code{POSIXct} instant. Its time zone is respected.}

\item{lat}{Observer latitude in degrees, between -90 and 90.}

\item{lon}{Observer longitude in degrees, between -180 and 180.}

\item{altitude}{Default \code{0}. Reference altitude in meters, from 0 to 15000.}

\item{visibility}{Default \code{50}. Meteorological visibility in kilometers,
from 20 to 131.8.}

\item{albedo}{Default \code{0.5}. Ground albedo, from 0 to 1.}

\item{prague_rgb_correction}{Default \code{TRUE}. Apply the Prague RGB calibration,
as in \code{\link[=calculate_sky_values]{calculate_sky_values()}}.}

\item{prague_rgb_correction_strength}{Default \code{1}. Nonnegative calibration
strength.}

\item{prague_rgb_correction_gain}{Default \code{"auto"}. Automatic calibration or
three positive RGB gains, as in \code{\link[=calculate_sky_values]{calculate_sky_values()}}.}
}
\value{
A list with \code{filename} (the installed full-altitude coefficient file),
\code{elevation_deg}, \code{azimuth_deg} (clockwise from north),
\code{angular_diameter_deg}, \code{rgb_gain} (the applied R, G, B gains), and the
reference \code{altitude}, \code{visibility}, and \code{albedo}. No native pointers are
included; the result can be serialized. The coefficient path is local to
this computer and should be resolved again on another machine.
}
\description{
Resolve the full-altitude Prague dataset and the Sun's topocentric position
without loading coefficients or generating an image. This lets rendering
libraries evaluate the model natively while using skymodelr's ephemeris and
RGB calibration through a public interface.
}
\details{
Install the data explicitly with
\code{download_sky_data(sea_level = FALSE)}. This function never downloads files
or prompts. It supports the visible-spectrum, full-altitude dataset and
checks the Prague solar-elevation range of -4.2 to 90 degrees. It does not
evaluate radiance, attenuation, or atmospheric refraction.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
info = get_prague_sky_metadata(
  as.POSIXct("2026-06-21 20:00:00", tz = "America/New_York"),
  lat = 40.7, lon = -74
)
info[c("elevation_deg", "azimuth_deg", "rgb_gain")]
\dontshow{\}) # examplesIf}
}
