% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_sky.R
\name{generate_sky}
\alias{generate_sky}
\title{Write a Hosek–Wilkie sky dome to EXR}
\usage{
generate_sky(
  outfile = NA,
  albedo = 0.5,
  turbidity = 3,
  elevation = 10,
  azimuth = 90,
  altitude = 0,
  resolution = 2048,
  numbercores = 1,
  hosek = TRUE,
  wide_spectrum = FALSE,
  visibility = 50,
  verbose = FALSE,
  render_solar_disk = TRUE
)
}
\arguments{
\item{outfile}{Default `NA`. Path to `.exr` file. If not given, the data will be returned instead.}

\item{albedo}{Default `0.5`. 0–1 ground albedo.}

\item{turbidity}{Default `3`. 1.7–10 atmospheric turbidity. Only valid for Hosek model.}

\item{elevation}{Default `10`. Solar elevation above the horizon (degrees).}

\item{azimuth}{Default `90`, sun directly east. Solar azimuth (degrees). Left side of the image represents North.
Halfway across the image represents South.}

\item{altitude}{Default `0`. Altitude of the viewer in meters. Valid [0,15000]. Only valid for the
Prague model.}

\item{resolution}{Default `2048`. Height of the image. Width is 2x this number.}

\item{numbercores}{Default `1`. Number of threads to use in computation.}

\item{hosek}{Default `TRUE`. Use `"prague"` to enable the Prague 2021-22 spectral sky model.}

\item{visibility}{Default `50`. Meteorological range in kilometres for Prague model.}

\item{verbose}{Default `FALSE`. Whether to print progress bars/diagnostic info.}

\item{render_solar_disk}{Default `TRUE`. Whether to render the solar disk in addition to the atmosphere.}
}
\value{
Either the raw data, or the data is invisibly returned if outfile is given. The EXR is written to `outfile`.
}
\description{
Write a Hosek–Wilkie sky dome to EXR
}
\examples{
if(run_documentation()) {
# Hosek model (default): clear morning, Sun SE, with solar disk
generate_sky(
  resolution = 400,
  elevation  = 15,
  azimuth    = 135,
  turbidity  = 3,
  render_solar_disk = TRUE
) |>
  rayimage::plot_image()
}
if(run_documentation()) {
# Same view but hazier and without the solar disk
generate_sky(
  resolution = 400,
  elevation  = 15,
  azimuth    = 135,
  turbidity  = 6,
  render_solar_disk = FALSE
) |>
  rayimage::plot_image()
}
if(run_documentation()) {
# Equal-area square mapping
generate_sky(
  resolution        = 400,
  elevation         = 30,
  azimuth           = 180,
  square_projection = TRUE,
  turbidity         = 3
) |>
  rayimage::plot_image()
}
# Prague model (may prompt to download coefficients on first use)
\donttest{
if(run_documentation()) {
generate_sky(
  resolution = 400,
  hosek      = FALSE,
  altitude   = 0,
  visibility = 80,
  albedo     = 0.2,
  elevation  = 5,
  azimuth    = 220,
  numbercores = 2
) |>
  rayimage::plot_image()
}
}
}
