# Convert Swiss ephemeris data to a starfield frame

Build a data frame compatible with `make_starfield_rcpp()` from Swiss
Ephemeris results.

## Usage

``` r
sweph_info_to_stars_df(sweph_info)
```

## Arguments

- sweph_info:

  List of planetary info objects.
