# Prepare Prague RGB correction gain

Prepare Prague RGB correction gain

## Usage

``` r
prepare_prague_rgb_gain(gain = "auto", strength = 1)
```

## Arguments

- gain:

  Default `"auto"`. RGB correction gain.

- strength:

  Default `1`. Correction strength.
