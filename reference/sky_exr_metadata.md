# Build EXR metadata for skymodelr RGB output

Build EXR metadata for skymodelr RGB output

## Usage

``` r
sky_exr_metadata(image = NULL)
```

## Arguments

- image:

  Default `NULL`. Image to read colorspace and white metadata from.
